# Component Gallery

Examples of styled buttons, selection controls, and sliders built on the experimental `@controls` library.

Run from the repository root:

```sh
cargo run --bin slint-viewer -- --auto-reload examples/component-gallery/gallery.slint
```
